interface PageRoutes {
  name: string;
  path: string;
}

type VoidFn = () => void;

interface HomeContent {
  name: string;
  text: string;
}
interface HomeCapture {
  title: string;
  text: string;
}
